import { Meta, StoryObj } from '@storybook/react';
import {{pascalCase title}} from '@ap/ui-kit';

const meta: Meta<typeof {{pascalCase title}}> = {
  title: '{{pascalCase title}}',
  component: {{pascalCase title}},
};

export default meta;

type Story = StoryObj<typeof {{pascalCase title}}>;

export const Default: Story = {};